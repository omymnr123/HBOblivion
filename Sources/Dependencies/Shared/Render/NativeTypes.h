// NativeTypes.h: Platform-neutral type aliases for window handles and instance handles.
//
// All type definitions now live in platform_headers.h (hb::shared::types namespace).
// This header exists for backward compatibility — include either one.
//////////////////////////////////////////////////////////////////////

#pragma once

#include "platform_headers.h"
